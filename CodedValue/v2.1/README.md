# CodedValue — v2.1

**Schema Pack Version:** 2.1.0
**Released:** May 2026
**Status:** Initial release

## Notes

Initial release of the CodedValue shared sub-schema targeting the v2.1 generalised model.
Includes `context.jsonld` and `vocab.jsonld` to ensure `code` and `display` fields are
properly mapped to IRIs when embedded in JSON-LD payloads. Restructured from
`generic-common/CodedValue/` to the standard `{SchemaName}/v2.1/` versioned folder layout.
